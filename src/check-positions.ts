import fetch from 'node-fetch';

async function checkPositions() {
    const accountId = '19876892'; // From logs
    const token = 'YOUR_TOKEN_HERE'; // I don't have the token, but I can try to get it from the backend if I run it there

    // Actually, I can just check the backend logs or run a script in the backend directory
}
